# 当前实施约束

- 用中文沟通。当前目标是 **macOS 风格前端质感、交互可靠性与桌面组件复用**，直接对接原版 Sub2API。
- 改动前先读 `docs/frontend/EXECUTION_PLAN.md` 和 `docs/frontend/DESIGN.md`，一次只执行一个任务卡。历史审阅报告中的网关、支付和部署风险不作为本轮前端实施前置任务。
- 保持 Vue 3、TypeScript、Pinia 和现有 API 契约，不换框架，不复制后端业务逻辑，不使用 `frontend-skill`。
- 不改 `server/`、根目录二进制、真实配置或旧原型，除非用户另行要求。演示数据只能进入明确的开发预览或测试夹具。
- 先理解调用链，再做最小改动；执行任务卡指定的检查。把实际变更、验证结果、未完成项更新回任务卡，不能把构建通过写成真实后端验收通过。
- 保留他人的并行改动，不覆盖整个工作区。所有工具窗口、临时服务器仅管理本次创建的实例。
