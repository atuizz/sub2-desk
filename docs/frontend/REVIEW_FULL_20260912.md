# 完整复核记录 · 2026-09-12

## 结论

本轮已完成代码分区复核、缺陷修复、全量隔离测试、生产门禁、26应用主页面及独立源码复建。已确认的问题修复后回归通过，可以继续本地交互和业务验收。仍不等同于全部官方功能100%覆盖或真实支付/升级/数据写入验收。

## 已修复问题

- 管理模块7类：无关字段覆盖、用量详情嵌套秘密泄露、Promo全量回写、卸载后密钥回填、详情ID错配、批量重试使用新草稿、统计筛选缺失。见REVIEW_ADMIN_20260912.md。
- 认证/支付：登录与退出竞态、缺少token/user仍采纳、TOTP登录正则、OAuth取消后迟到登录、解绑跨账号回写、支付回调重复下单及恢复归属。见REVIEW_AUTH_PAYMENT_20260912.md。
- 共享HTTP客户端：请求记录会话快照；旧401不得清理新会话或使用新身份重放旧请求；刷新过程中换身份同样拒绝重放。6项专项通过。
- 钱包：未知下单结果按用户锁定，刷新/重开不直接重下；先核对订单，再显式确认新建。明确拒绝可重试，成功和换号状态受代次约束。wallet-unknown-order.test.cjs包含在默认测试和源码包。
- 生图/运维/插件10类：跨密钥归属、刷新丢幂等标识、取消/部分删除失败保留、FileReader及下载资源清理、错误形状、插件TOTP旧响应、兼容元数据缺失、风控筛选竞态、清理反馈及WS销毁后旧回调。见REVIEW_OPS_BATCH_20260912.md。
- 组件5处模板回调补显式参数类型，使不同入口的严格类型检查一致通过。
- 测试/交付：修复旧M03夹具；默认test收集全部测试，包内收录全部测试；生产浏览器测试重置其专用夹具会话，避免上次模拟登录造成假失败；刷新100项生产依赖许可声明；README及发行记录不再覆盖为过期摘要。

## 验证证据

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| 全scripts测试及core窗口测试 | 476项：475通过、1可选public浏览器跳过、0失败 | output/review-20260912/all-tests-final.txt |
| 可选public浏览器独立启用 | 27/27通过、0跳过（与上述已有测试重叠，不相加） | output/review-20260912/public-browser.txt |
| 支付SDK/降级/公开恢复浏览器 | 23组PASS，0真实支付、0外部请求 | output/review-20260912/payment-browser.txt |
| Core、console标准类型检查；运维窄入口 | 全部exit 0 | ops-typecheck.txt及本轮工具记录 |
| 共享交互 | Enter/Escape/Tab、前台层、多窗口、390px、分页与删除取消通过 | scripts/frontend-browser-check.js |
| 全应用主页面 | 26应用，浅深色/390px，无破图、pageerror、根溢出、业务写入 | scripts/all-apps-browser-check.js；output/playwright/release/all-apps |
| 批量生图关键交互 | 9场景通过，包含取消失败确认层、Escape；写入仅夹具 | scripts/batch-image-browser-check.js |
| 生产构建门禁 | 7场景：unlocked/ui-lab不绕过认证、不暴露开发句柄、登录后恢复应用 | scripts/production-browser-check.js |
| release构建 | 591模块，exit 0 | 构建工具结果 |
| 独立源码目录 | pnpm10.28.2锁定安装成功，类型检查、475通过1跳过、release构建通过 | clean-install.txt、clean-all-tests.txt、clean-build.txt |

独立复建目录为output/review-20260912/clean，由生成源码归档解压而来。随后补入最终test runner及钱包测试（未改变生产代码），对两个src树做SHA-256逐文件比较：0差异。最终源码包包含上述补入文件。当前机器全局pnpm为11.19.0，复建特意使用项目指定10.28.2；没有替换当前工作区依赖。pnpm store索引不完整导致licenses命令失败，改为递归读取实际安装生产包声明生成许可清单，未将索引失败写为依赖安装成功。

浏览器控制台预设503用来验证错误状态，不是线上事故；测试中的“配置已保存/订单已创建”均由本地夹具响应。生产门禁测试首次重用已模拟登录的会话产生假失败，现测试每次清空专用会话再检查并通过。公共浏览器首次缺少对应Playwright浏览器版本，改用已安装Chrome后通过，不降低断言。

## 剩余边界

- 未执行真实安装、账号/密钥修改、生图计费、支付/退款/Webhook、邮件发送、风控解封/清理、备份恢复或官方后端升级。需在隔离的真实测试实例继续业务验收。
- 当前后端0.2.1与参考源码0.2.4不能仅靠页面存在证明全部兼容；已实现的feature flag/缺接口失败状态保留。
- 无后端幂等协议、ETag或快照事务时，前端保护不能保证跨设备全局下单幂等、并发同字段不覆盖、同总数CSV快照绝对一致。
- 仍有细节覆盖差异：监控滚轮缩放/查询参数恢复、部分用量分组/端点/余额历史视图、部分平台特殊账号选项等见PARITY各卡。固定官方已删除的Session Token处理不伪造为可用。
- Docker/nginx运行环境未实际启动；源码和静态包本地可复建不等于生产部署通过。Apple素材仅本地public，不属于可分发原创资源。
