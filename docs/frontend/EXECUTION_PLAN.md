# 前端改造执行计划与交接

## R11 · Sub2 Desk 开源与品牌收口（2026-09-16，完成）

用户授权补齐发行白名单、开源仓库门禁和品牌重定位。产品显示品牌统一为 Sub2 Desk，明确为独立的 Sub2API 桌面式控制台；不改包内 `@sub2-mac/*` 技术名称，不改 server、真实配置或业务数据。公开发行只使用 `public-release` 原创素材，默认本地预览继续保留现有桌面素材。

本卡验收：源码包解压后依赖完整、测试/类型/默认与release构建通过；GitHub Actions 可在干净环境复跑；README/CHANGELOG/品牌说明与当前结果一致；公开仓库不包含运行数据、凭据、私有备份或本地素材。Docker 仍准确标注为前端镜像，未把没有 Docker 引擎的本机检查写成容器运行验收。完成后再创建公开仓库与发布首个 release。

实际交付：公开仓库 https://github.com/atuizz/sub2-desk，品牌名称、SVG/PNG/ICO网站图标、锁屏与系统信息统一；补齐源码测试辅助模块、前端Compose、CI、贡献/安全报告模板与品牌文档。577个公开文件通过已知本地凭据及禁止路径扫描；两处私钥格式是明确的假数据测试夹具。私密漏洞报告已启用。

验证：本地类型与release构建通过；Windows干净源码1061项1058通过3跳过。Linux公开检出CI 35035651585成功：1061项1057通过4跳过（Windows运维专项、两项可选上游检出、可选浏览器）；类型、release构建、真实Docker镜像构建与页面/SPA/API/health/缺失资源404检查通过。Docker上游为隔离HTTP夹具，不冒充原版业务或真实支付验收。首次源码提交 f7e52b9，后续收口仅更新文档与发行证据；源码与静态归档通过快照绑定，发布入口为仓库Releases。

边界：提供已有Sub2API后端的前端一条Compose启动命令；未实现后端/数据库/Redis/TLS的一键全栈安装，未部署到用户生产域名。第三方支付、邮件及OAuth实联仍未验收。历史任务卡中的未发布/无Docker记录描述当时状态，以本卡为当前发行结论。

## R10 · 全功能审阅缺陷修复（2026-09-14，完成）

用户授权修复output/full-functional-audit/REVIEW.md中的12项确定问题及账号owner候选。本次只执行本卡，按不重叠文件分线：公告/代理、管理员增量操作、设置/身份、用量/密钥/账号；不改server、真实配置或业务数据。复用现有MacSheet/表单/反馈组件，新增公告受众和阅读状态入口须有加载、空、失败、小屏状态；错误恢复保护草稿和未知写结果，不移植后端业务。

验收：每项对应反例转为正确行为回归，既有全量、core/console类型、默认与release构建、真实后端只读支付设置/空趋势复查及新增公告界面隔离浏览器验收。真实邮件/支付继续免验。实际结果、改动与剩余项在本卡补记，旧发布包不代表新代码已验收。

已完成F1/F2、A1–A5、S1–S5：支付null规范化及防御渲染、合法空趋势、公告Unix秒/差异清空和受众/阅读入口、余额与订阅增量幂等及持久未知结果保护、代理跳过反馈与迟到隔离、设置草稿恢复/正常token轮换、资料会话发布校验、部署接入地址、CSV分页完整性检测。账号marker按owner隔离，存储失败禁新增；历史无owner记录不自动归属或删除，单列显式核对路径。公告编辑底部按钮固定，小屏可达。保留现有MacSheet/MacButton与本地素材。

最终全量1061项：1060通过、1可选跳过、0失败；core及console类型通过；默认/release构建通过。新增R10专项176项（公告代理20、增量60、设置身份13、数据账号83）；旧夹具仅补新依赖/挂载流程，未为通过测试放宽产品保护。真实后端只读：27应用最终扫描无加载失败/未捕获异常，支付空配置勾选及分类切换正常，空用量不再报错；模型广场关闭导致404按配置状态保留。浏览器夹具：公告定向定时提交、阅读分页搜索排序失败恢复、浅深/390px；金额防重Web Locks和IndexedDB两模式下响应未知/刷新/直接API重试均受保护，读取不自动解锁。真实业务写入0，未真实支付/邮件/供应商联调。

详细映射、证据与边界见output/r10-fixes/HANDOFF.md。本轮辅助任务及自建浏览器均结束，现有8000/5173保留；未部署/发布/重打归档包。当前开发页面使用新源码，旧release归档未包含R10，不得当成新修复包。CSV检测不是数据库事务快照，未知金额操作仍需管理员核对记录，真实业务写验收不由夹具替代。

## R09 · 发布前审核与上线准备（2026-09-14，本地审核完成，等待部署/发布目标）

用户授权整体发布前审核，审核通过后上线部署并发布。目标服务器/域名/后端origin/发布仓库待用户明确；先完成本地可审阅结果，不将其他项目主机当作默认部署目标。支付/邮件实联按既有决定不作门禁。范围包含最终源码包、依赖/许可证、Docker构建上下文、nginx隔离生产入口及核心交互；发现确定缺陷先修。当前已确认Dockerfile缺少release-snapshot模块及快照文件输入，需要补齐。

已修Docker COPY缺失与HTTPS上游SNI，静态响应补nosniff/no-referrer/隐藏版本；发行包补第三方随附许可全文及私密文件排除。生产依赖audit已知漏洞0。全量885项884通过1可选跳过，core/console类型与release构建通过。Docker COPY目录独立锁定安装/类型/构建通过（本机无Docker引擎，不冒充镜像实跑）。真实nginx22项通过，含现用0.2.4只读转发；临时服务已回收。新版可分发包output/releases/1.0.0-20260913-184251，源码535文件扫描无私有运行文件/已知真实密钥；干净源码885项882通过3可选跳过、类型/构建通过，234静态文件哈希0差异。3个上游npm包缺完整版权文本已明确列来源边界。未指定上线服务器/域名/后端origin及发布仓库，未远端部署、未公开发布。后续收到目标后按已授权流程继续，不重复询问发布权限。详见RELEASE_PREPUBLISH_20260914.md与output/prepublish-audit/HANDOFF.md。

## U04 · 本地运维脚本收口（2026-09-14，完成）

用户授权修复复核发现的三处运维问题：start-all统一现用backend-runtime；升级/预检脚本拒绝覆盖既有备份与重复切换；回滚先保存现状、按事务恢复数据库、恢复失败不自动启动旧程序。原8000/5173保持运行。检查包括PowerShell语法、故障注入、实际只读/幂等启动、新旧归档哈希与最终停服备份的独立完整恢复。具体资料output/operations-closeout。

26项离线脚本断言通过；启动-CheckOnly及正常模式均确认正确目录/0.2.4并复用PID35520/33932。回滚-CheckOnly通过，重复upgrade和preflight实测在停服/数据库访问前被拒绝。最终private-cutover.dump已事务恢复至唯一临时数据库，余额/数量及四表哈希与升级前匹配，临时库已删除；原备份和业务库不变。PowerShell环境变量空值恢复问题由测试发现并修复。版本升级报告中的“13项核心读取”准确口径为12次GET+1次登录POST。说明见docs/LOCAL_OPERATIONS.md。

全量883项882通过1可选跳过，默认/release构建通过。新桌面包output/releases/1.0.0-20260913-181007、新可分发包1.0.0-20260913-181149包含启动器、运行工具、故障测试和运维说明；私有回滚脚本/备份不入包。源码归档独立解压后26项脚本测试通过，531文件哈希一致，无后台的源码包拒绝误启旧原型。应用源码与R08一致，不把运维脚本测试冒充真实现用数据库回滚；原PID与运行版本保持。交接output/operations-closeout/HANDOFF.md。

## U03 · 现用后端升级测试与切换（2026-09-14，完成）

用户明确授权“测试通过后切换当前后端”。官方latest页面于2026-09-13核对指向v0.2.4，GitHub API403限流；使用官方checksums验证的Windows发行包。现用数据库备份恢复至独立sub2_upgrade_probe_20260913，暂停副本上游账号，58007+独立56379缓存完成0.2.1→0.2.4及数据库备份恢复→0.2.1回滚测试。各版本登录和13项核心读取通过，余额/数量保持；groups.models_list_config→model_allowlist为官方235/236迁移，原值保留。

测试通过后实际停止8000、再次pg_dump、替换output/backend-runtime/sub2api.exe并重新启动，现场与5173代理均读到0.2.4，配置哈希不变。首次操作遇Windows端口释放延迟，自动恢复旧程序后补等待重试成功。当前PID35520；2026-09-14再次验证登录/13项读取与核心数据一致。真实浏览器4项通过：登录、账号列表、更新页显示0.2.4、无未捕获错误。没有真实支付/供应商调用测试。GitHub403及Windows官方restart不拉起程序，应用内自动升级未被标为通过。

证据/私有备份在output/upgrade-test-20260913，说明见UPGRADE_RESULT.md。现用8000/5173保留，测试58007/56379已停止；独立副本数据库保留供追溯。旧“本地0.2.1”报告为升级前历史。前端源码/发行包未因本次后端升级而改变。

## R08 · 跨刷新防重、草稿保护与小铺布局（2026-09-13，完成）

用户授权补齐剩余前端收口项，并修订截图中小铺设置布局。联合任务分文件并行：商业/收款提供方持久化未知结果保护；桌面关闭/刷新及编辑表单草稿保护；主任务负责小铺页面和设置主草稿接线及集成。真实邮件/支付联调按用户要求不作为验收条件，仅验证代码、官方契约和隔离交互；不改server、真实配置/业务数据。官方部分续退后台限制及外部升级下载不在前端修复范围。

小铺设计：保留现有设置侧栏，缩短说明文字；店铺身份/访问方式与名称/地址输入分层，统一对齐、尺寸和动作位置，390px纵向可操作。空/读取失败/保存中/修改未保存/不受支持内嵌均保持清楚反馈；保留catfk与16688默认适配及其他店铺外部打开。实际店铺链接不改。

检查：各线反例测试，窗口红灯/菜单/快捷键/刷新草稿保护、取消恢复、跨刷新/账户隔离与存储失败防重；小铺1440浅深及390px浏览器；最终全量测试、类型、双模式构建及候选包快照。真实写入全部使用夹具。

代码/契约与隔离交互收口：全量882项881通过1可选跳过，公共浏览器单独32/32；core/console类型通过。草稿28项、小铺14项、付款22项（Web Locks与IndexedDB各运行一次）真实Chromium夹具通过，无脚本异常和真实业务写入。小铺名称/地址对齐、访问方式分层、侧栏专属图标、切分类草稿保留；主要编辑表单红灯/快捷键/刷新保护接线。支付操作持久标记无秘密，正常令牌轮换兼容已加真实Axios反例。详情RELEASE_R08_20260913.md及output/r08-closeout/payment.md、drafts.md。

默认/release构建通过。桌面预览包output/releases/1.0.0-20260913-070424，可分发包1.0.0-20260913-070533；均source/static归档及SHA256。可分发源码干净锁定安装/类型/构建通过，882项879通过3可选跳过（公共浏览器+两项未分发的官方源码对比）；234静态文件复建哈希0差异。成品包额外4项浏览器门禁和草稿交互通过。打包后验收伴随文件RELEASE_NOTES.md、validation.json提供最终结果，归档内任务记录保留打包时阶段。本次5199预览和测试浏览器回收，原5173/8000保留。官方部分续退后台限制仍保留；真实支付/邮件按用户要求不作门禁。

## R07 · 完整审阅缺陷修复（2026-09-13，本轮收口完成）

用户授权快速并行修复本次完整审阅确认的问题。本卡分文件组执行：认证/合规/支付恢复、账号创建与批量向导、商业计划/退款/兑换/订阅、设置秘密字段与支付渠道、用户密钥/风控/运维/Markdown；主任务负责菜单栏可访问性、发布证据绑定与集成。范围以 output/final-audit 下三份审阅报告为准。保留既有 macOS 素材、玻璃效果、三屏账号向导和官方 API 契约；不修改真实配置或业务数据。

验收：每条已确认反例对应回归，全部既有测试、两包类型检查、默认与 release 构建、关键表单真实浏览器夹具与浅深/390px检查，更新可复建发布包。外部支付/OAuth 无测试配置，继续明确未验收；历史核心后端验收不得自动继承到源码已变化的新包。实际结果与剩余项由主任务收口后补录。

6项P1/10项P2前端修复落盘，详见RELEASE_FIXES_20260913.md。全量836项835通过、1可选跳过；公共页含浏览器单独32/32通过。core/console类型通过。主流程13、运维正文10、商业15、设置19项Chromium交互夹具通过，26应用浅深/390px首页检查无异常/破图/根溢出。浏览器额外发现并修正合规弹层被窗口遮挡，以及数字分组输入自动转number的兼容问题。默认/release构建及生产入口7项浏览器夹具通过。desktop包output/releases/1.0.0-20260913-034823；可分发包1.0.0-20260913-035013。可分发源码包干净锁定安装/类型/构建通过，836项833通过3跳过（公共浏览器及2个未随包分发的官方checkout比较）；235个复建静态文件逐哈希0差异。独立副本变更源码后打包被正确拒绝。两套包快照都与当前源码/对应素材一致，历史核心后端验收未套用，coreBackendAccepted=false。

全部业务写入仅夹具，未修改8000实例。官方0.2.4后端拒绝部分退款续退，此项不能因前端入口补齐而标业务通过。草稿保护已复用到本轮主要表单，不宣称所有窗口关闭路径全部覆盖。两套归档旁RELEASE_NOTES.md/validation.json补充打包后完成的复建验收；归档内任务记录保留生成时阶段。最终交接见output/final-fixes/HANDOFF.md。本次预览5198和自建浏览器回收，现有5173/8000保留。

## G02 · 顶栏背景折射（2026-09-13）

新增core/MacMenubarGlass，分离文字与背板，通过SVG feDisplacementMap及本地生成64px PNG位移图采样真实backdrop，上下边缘相反位移，中心近中性，再轻微模糊/透色/边缘高光。无额外持续动画循环，无远端资源。减少透明度使用实色；不支持滤镜时保留磨砂。Chrome菜单打开/Escape与390px无横向溢出检查通过。条纹真实背景同区域对照：启用位移与仅模糊的25200通道样本中23025不同（output/refraction-on.png、refraction-off.png），确认当前Chromium确实重采样背景；不宣称所有浏览器或苹果系统合成等价。初次对照条纹误加在App层叠上下文之外而遮住顶栏，纠正为同父层后测试有效。截图menubar-refraction-light/dark.png。类型及构建收口。

## G01 · 菜单栏与Dock玻璃材质（2026-09-13）

调整浅深材质梯度、透色/模糊、边缘高光和阴影，Dock沿现有鼠标物理交互加入局部反光，不额外建立动画循环；装饰层pointer-events:none，图标层级保留。减少动态/透明度和无backdrop-filter有降级。类型及默认构建通过；浏览器验证1440浅深、指针反光、390px无Dock溢出，截图output/glass-light.png、glass-dark.png。不声称实现苹果系统合成器或真实背景折射；这是网页视觉近似，后续效果仍需用户评价。未改业务接口或偏好。

## T01 · 刷新棕黑色首屏修复（2026-09-13）

入口body残留固定#20181b导致模块/壁纸加载前出现棕黑底。移除该类，增加最早执行的只读主题恢复（浅/深/系统/旧isDark），首屏使用匹配的中性色；store偏好同步保存，避免切换后立即刷新读取旧值。保留用户壁纸和深色选择，不强制浅色。theme-startup专项覆盖四种偏好与无效存储；类型检查通过，默认构建验证。未修改业务数据或重新打发布包。

## R06 · 发布五项执行（2026-09-13）

按用户授权补账号/监控/用量/历史/Markdown/定价差异，修上传最终JSON与nginx256MiB。独立官方0.2.4校验发行包+PostgreSQL真实核心17项、生命周期7项、真实前端4项、nginx最终生产6项通过；模型服务受控、Redis协议测试依赖。用户无外部支付/OAuth沙箱，明确列未验收。官方校验包0.2.1→0.2.4及pg_dump/pg_restore回滚到0.2.1通过。GitHub API403使自动在线升级未下载；Docker/正式TLS拓扑/生产Redis未运行验收，不能冒称五项全部生产Go。

全测727项726通过1可选跳过，类型/release构建通过；候选包干净锁定安装/类型/构建通过，725通过2可选跳过（额外为缺可选上游checkout），257源码哈希0差异。完整证据与限定支持声明见RELEASE_CLOSEOUT_20260913.md、release-verification.json。测试只操作output/release-validation独立端口/数据库，当前8000业务实例未升级或写入。

## E02 · 原生分屏账号助理（2026-09-13）

新增账号改为选择平台→连接账号→确认添加三屏，保留编辑单屏。各屏使用v-show保留草稿和授权组件，返回不丢链接；前进校验必填及真实buildCredentials，底部操作固定，换屏焦点回标题及滚动复位。代理/高级连接与批量授权保留在认证屏，分组/并发/模型配额位于确认屏，无删减API或写操作。文件导入增加选择→预览→结果阶段指示，保留多文件及部分失败处理。

验证：677项通过、1可选浏览器跳过；额外22项导入专项通过；类型检查通过。account-editor-browser-check真实DOM夹具通过平台/必填/OAuth复制/返回保留/最终确认/390px与Escape，无真实账号创建。截图output/account-editor-final/wizard-platform.png、wizard-confirm-mobile.png。默认构建日志wizard-build.txt；旧发布归档未含本次E02。

## E01 续作收口 · 2026-09-13

账号列表、添加平台卡片/必填/固定保存、OAuth可见链接复制、小铺独立图标已实现。MiniMax及国产协议配置/Grok媒体资格、日志持久化/隐藏排行/beta策略、未知路径等最新版确定缺口已补。详见ACCOUNT_EXPERIENCE_FINAL_20260913.md与CURRENT_UPSTREAM_20260912.md。普通浏览器夹具已核验，真实后端启动恢复并返回0.2.1，未升级为官方最新0.2.4。最终测试/构建日志output/account-editor-final，真实业务验收仍单独记录。

## U02 · 日志、设置规则与基础类型收口（2026-09-13，本线已完成）

续接中断并核实落盘：G01补persist_access_logs回显/保存及未知字段禁写；G03补AccountPlatform/GroupPlatform、MiniMax分组选择和阈值入口，PlatformQuotaLimits保持五平台；G04隐藏排行差异保存及Beta/Fast独立规则表单完成，保留未知字段、失败草稿和迟到响应隔离；G07统一model_allowlist/Codex manifest基础类型及可选字段。上一轮settings/groups/system三份API SHA-256全部一致，冻结不变。

最终全量635项：634通过、1可选公共浏览器跳过、0失败；类型检查及独立目录生产构建通过（613模块）。独立5197浏览器27项通过，1440浅深/390px截图已复核；10次写请求均为夹具，真实业务写入0。测试Vite及新建browser context已回收。不改AccountsApp/OAuth/main/App，账号线与未知路由由父任务继续。详细文件、契约、日志及限制见UPSTREAM_GAP_FIXES_20260913.md；本线完成不等于真实后端业务验收。

## U01 · 当前官方稳定版与契约核对（2026-09-12，E01 独立子项已交付）

独立源码在 output/upstream-current-20260912：官方 latest stable=v0.2.4，标签 commit=5de5e2be；frontend tree 与旧参考98d86915完全相同，仅两提交的backend VERSION不同，已用发行工作流解释。旧参考未改。核对64条官方路由及API，修复分组模型候选endpoint、settings的MiniMax阈值/字段类型、system已最新响应类型，仅改三份API文件及独立回归脚本。

118项隔离测试、类型检查、608模块独立目录构建、14项更新浏览器夹具通过；5196测试实例已停止，真实业务写入/升级/重启为0。本机8000公共version两次连接拒绝，当前版本未核实，历史0.2.1不能代替本次现场证据。账号Grok媒体资格/MiniMax、设置入口及404等精确缺口见CURRENT_UPSTREAM_20260912.md；其中日志维护payload漏persist_access_logs会使官方整份保存重置为false，需父任务优先修UI接线。本线未改auth/client/accounts/App/main，不标记全量业务兼容通过。

## E01 · 账号工作台、添加账号引导及独立小铺图标（2026-09-12，进行中）

用户改回本任务继续；新任务日志确认模型服务403余额错误停止，未继续并行编辑。主任务负责Accounts列表/添加弹窗/OAuth显式链接及集成；独立子任务负责小铺图标和官方最新版本契约核对。保留导入拖拽、防重提交、差异保存、迟到响应隔离。以普通Chrome的实际操作、1440浅深/390px、全量测试和默认/release构建收口，不把内置浏览器白屏归为卡网全面禁止iframe。

## D03 · 店铺默认适配（2026-09-12）

按用户要求，HTTPS catfk.com/www.catfk.com 与16688.com.cn/www.16688.com.cn的/shop/:id链接默认内嵌；其余链接中性外部打开，不尝试iframe、不显示加载或错误横幅。设置自动显示访问方式；同源、混合内容和非标准端口仍不内嵌。未新增或替换真实店铺配置，仅改变展示策略。47项小铺测试、类型检查通过；真实WOAI页面回读DOM确认iframe=0、alert=0、loading=0、购买链接存在。支付最终跳转不在适配承诺内。

## D02 · 现场应用打不开（2026-09-12）

现场5173连接拒绝，已重新启动Vite；真实管理员会话打开仪表盘并读取到后端实际统计。WOAI店铺iframe真实返回403，正文Denied by http_bot_simple（ESA）；没有绕过防护，改为显式店铺外部打开页面，保留兑换入口，避免空白。小铺46项测试通过。第三方外部浏览器是否放行仍由对方决定。

## D01 · 小铺购买与原生文件交互（2026-09-12，已实现）

用户授权：设置店铺链接后增加购买应用，购买后兑换；账号平台视觉层次；桌面及账号窗口拖入JSON导入。使用原版custom_menu_items保存小铺条目（不加后端字段），保留其他菜单。独占分工：主任务桌面/入口/设置接线，账号线Accounts及导入预览，小铺线store/配置面板/应用。iframe禁止嵌入时保留外部打开，不绕过安全策略；所有实际购买和导入由用户确认，验收使用夹具。

已实现：管理员设置→小铺与兑换，支持多店铺；保存后Dock/启动台/访达动态出现小铺，未配置隐藏，刷新/身份变化隔离。浏览器式窗口显示当前域名、刷新和外部打开，提供兑换卡密入口；跨域iframe保留必要脚本/表单/存储/弹窗权限，禁止自动顶层导航，同源及HTTPS→HTTP仅外部打开。

账号平台以真实上游图形和辨识色呈现，桌面及账号窗口都接受JSON拖入；最多10文件、单文件20MiB、合计50MiB，合并前校验官方导出结构、重复及代理冲突。只展示数量/平台/类型，确认后发送一次导入；部分失败/未知结果不伪造成功或重放整文件。文件选择复用同流程。

验证：全测试543项（542通过、1可选公共浏览器跳过），类型检查通过；浏览器9场景通过，覆盖动态入口、内嵌/外部打开/刷新/兑换、桌面与窗口drop、凭据不展示、确认后仅1次导入、非JSON/非管理员拒绝。浅深/390px截图output/desktop-experience，源码及限制见DESKTOP_ACCOUNTS_20260912.md和DESKTOP_CARD_SHOP.md。没有真实保存店铺、购买或导入账号。第三方店铺可否内嵌/支付取决于其CSP/XFO/第三方cookie政策，外部打开始终保留。

最终默认生产构建通过（608模块），5173入口HTTP200。本轮未重打发布归档，当前代码与dist已更新，旧归档不包含D01。

## R05 · 完整复核（2026-09-12，完成本轮）

详情见REVIEW_FULL_20260912.md。已修复分区复核发现的竞态、秘密展示、覆盖写入、跨密钥、未知下单、旧401及错误反馈问题。默认测试覆盖全部476项：475通过、1可选跳过；可选公共浏览器已单独27/27通过，支付23组与生产门禁7场景通过。Core/console/运维窄入口类型通过；26应用主页面复核及干净源码锁定安装/测试/release构建通过。真实业务副作用/后端升级及部分官方细节覆盖仍不在完成声明内。

## F15 · 首次安装与批量生图入口（2026-09-12）

安装向导实现完成：7项隔离测试和7项浏览器场景通过，见PARITY_SETUP.md。批量生图按官方实际任务管理页补齐，已接manifest/官方两条路径与Safari入口；20项隔离测试及主任务7项浏览器场景通过（创建/自动详情/重开详情/Escape取消确认/390px），见PARITY_BATCH_IMAGE.md。后端只读/setup/status返回needs_setup:false。不得将本卡夹具写为真实安装或生图成功。

## F16 · 高级功能补齐集成（2026-09-12）

- 策略：渠道功能可视化、用户属性筛选/批量/创建联动、分组多媒体定价及Codex manifest；见PARITY_POLICIES。
- 账号：OpenAI RT/mobile RT/Codex Session/Agent Identity/PAT和Anthropic Cookie批量授权；见PARITY_ACCOUNTS。Session Token已被参考官方隐藏且无可核实处理，不编造接口。
- 运维：邮件/运行/高级设置、审核密钥池/解封/hash缓存、WebSocket/轮询、分布/容量视图；见PARITY_OPERATIONS。主任务另做邮件弹层6场景真实DOM夹具通过。
- 公共页/支付：法律语言、Markdown目录复制、公开模型完整价格对比、微信确认降级与支付宝回退；见各分卡。
- 用量：错误记录、趋势/模型/排名、列配置、导出/清理与Promo异常隔离；见PARITY_USAGE。
- 全量隔离测试由`node scripts/test-parity.cjs`运行，当前356项：355通过、1可选浏览器跳过、0失败；独立公共页浏览器27项和支付23组夹具另见分卡。全项目vue-tsc通过。26应用主页面浅深/390px已巡检通过；后续新增复杂弹层的覆盖不由主页巡检代替。
- 原始证据output/parity-completion/verified-features-20260912.txt、setup-mobile.png、alert-email-mobile.png、batch-create-mobile.png。未做真实生图、支付、安装、管理员写入或升级；本轮不标记全量官方业务验收。
- 最终默认生产构建通过（591模块），新增安装、批量、策略和运维代码已包含在dist。截图已复核，新增表单在390px使用内部滚动且底部操作可达。源码包脚本会带上本轮PARITY报告和专项测试；本轮没有重新发布归档或执行部署。

## W01 · 高清壁纸接入（2026-09-12，完成）

用户提供的3张1672×941玻璃壁纸保留原尺寸，新增网络6K源派生的3张3840×2160 Tahoe壁纸，旧壁纸不覆盖。接入设置选择器、右键轮换、持久化白名单，480×270懒加载缩略图；Apple下载素材仅本地public启用。类型检查、生产构建通过；独立浏览器验证6张切换、解码尺寸、保存及刷新恢复通过。来源/体积见PARITY_WALLPAPER.md与output/wallpapers/manifest.json；截图output/wallpapers/desktop-preview.png。不涉及业务API变更。

## F08–F14 · 官方功能补齐

2026-09-12续检：补上账号策略模块的隔离测试依赖，账号专项22/22通过；六条功能线合计199项，198通过、1项可选浏览器测试跳过、0失败。全项目vue-tsc通过。主入口已接入官方桌面深链接、支付返回识别、未读公告弹窗；Commerce兑换码删除独立确认，优惠码详情不再误绑定兑换码行。壁纸核验见PARITY_WALLPAPER.md，尚未生成4K新图。并行任务曾因503/并发限制中断，安装向导、用量专项和最终交付仍未完成，不可标记完整官方对齐。

本轮最终验证：新增2项深链接/跨域回跳隔离测试通过，总计200通过、1跳过。Vite生产构建通过（530模块）。all-apps-browser-check.js覆盖25应用浅/深色及390px，0脚本异常、0破图、0根容器溢出、0业务写入；HTTP错误为预设503失败夹具，不能称真实后端成功。截图位于output/playwright/release/all-apps，专项日志output/parity-completion/regression-20260912.txt。测试服务器为本次启动的5181。

用户已授权继续并行补齐，参考0.2.4固定源码；原版跳转不计原生覆盖。
各线独占认证、账号、渠道/分组/用户策略、支付、运维/插件、公共页/公告，主任务负责管理员用量、路由入口和最终集成。
恢复点：output/checkpoints/frontend-before-parity-completion.zip。状态：实施中，逐线留下PARITY_*.md与隔离测试，最终再核对全部未完成项。

## M01–M05 · 限时并行原生化与功能对齐（2026-09-11）

用户授权多线并行，当前轮次覆盖40–60分钟。各线独占文件范围，禁止覆盖他人改动；最终由主任务统一构建与验收。

| 任务卡 | 范围 | 状态 |
| --- | --- | --- |
| M01 | 用户应用原生布局及功能恢复 | 本轮实现已集成；功能缺口见独立RESULT及UPSTREAM_PARITY |
| M02 | 管理应用原生布局及关键操作闭环 | 本轮实现已集成；功能缺口见独立RESULT及UPSTREAM_PARITY |
| M03 | 账户安全、认证和系统设置 | 本轮实现已集成；功能缺口见独立RESULT及UPSTREAM_PARITY |
| M04 | 上游版本/路由功能清单及更新核验 | 本轮实现已集成；功能缺口见独立RESULT及UPSTREAM_PARITY |
| M05 | 共享桌面、集成验收和可运行交付 | P0回归通过，见M05-RESULT.md；非完整官方功能验收 |

恢复点：output/checkpoints/frontend-before-parallel-20260911.zip。所有线只修改前端，不执行真实支付、删除数据或升级后端。

## I03 · 网站 favicon（2026-09-11）

- 用户明确指浏览器网站图标。原 favicon 错用 keychain.svg，现替换为蓝色玻璃质感的 Sub2-Mac S 标识。
- 提供 SVG、16/32/48px ICO、180px触屏图标，普通与release素材目录同步，URL带版本避免旧缓存。
- 验证：生产构建通过；当前浏览器 DOM 已确认 ICO/SVG 指向新 favicon，包含缓存版本号。

## I02 · 仪表盘、风控、账号图标重设计（2026-09-11）

- 用户否定原有速度表、锁孔盾牌和钥匙串的语义；改为多指标面板、风险记录审查、多账号身份卡及管理控件。
- 范围：3个SVG源文件、对应PNG、图标注册表、审阅页；不改其余21个图标与业务逻辑。
- 验证：类型检查、默认生产构建通过；审阅页03/09/13均已更新且浅深底加载成功。

## I01 · 逐个图标质感与语义复核（2026-09-11）

- 范围：24个应用图标、各入口及页面中对应应用的图标引用；保留原生同义功能素材，对业务专属图标使用实体材质造型。
- 顺序：逐项检查来源/语义 → 必要修订 → 32/64/128px浅深底验证 → 所有入口一致性 → 类型/构建/真实页面复核。
- 状态：已完成24项逐个审阅；保留16、修订8；每个32/64/128px浅深底图已核验，浏览器24项加载通过，类型/构建通过。详细记录见 ICON_REVIEW_2026-09-11.md。禁止再次用统一底板加粗线框替换原有图标；不改业务接口。

## L03 · 开发者中心底部留白（2026-09-11）

- 现场确认：最近使用日志、快捷操作两块 computed padding 均为 0px；p-4.5 未受当前 Tailwind 配置支持，且两块未接入 app-panel。
- 修改：仅两块卡片接入共享 app-panel，统一16px内边距与标题栏间距，去除日期额外偏移。
- 验证：typecheck 与默认生产构建通过；真实页面两块 computed padding 均为16px、标题栏均为44px，截图确认标题/日期/查看全部和快捷按钮不再贴边。保留日期范围及所有应用跳转。

## L02 · 恢复原有桌面图标与壁纸（2026-09-11）

- 用户反馈：简化线框图标粗糙，原生质感丢失。
- 修改：默认使用原有 public 素材；release 模式单独使用 public-release，源码发行包缺少原素材时使用可分发资源。登录玻璃修复保留。
- 验证：typecheck、默认生产构建、打包脚本语法检查通过；当前真实浏览器已核验原图标/壁纸恢复，登录会话保留。只调整素材选择与打包入口，不改业务数据。

## L01 · 登录页玻璃材质修订（2026-09-11）

- 用户反馈：卡片和头像都偏白，缺少液态玻璃质感。
- 根因：MacLockscreen 的局部样式将卡片覆盖为 96% 白底、输入框覆盖为纯白，白色图标对比不足。
- 范围：仅 MacLockscreen.vue；移除不透明覆盖，增加透色、磨砂、边缘高光，默认头像使用蓝色层次，恢复输入和登录按钮的对比。
- 验证：typecheck 与生产构建通过；当前真实页面热更新后截图核验通过，输入焦点可见；390px 视口卡片 x=35、宽320、右边界355，无溢出。保持原有登录/注册/2FA 调用逻辑，本轮未提交登录或修改后端。

## 交付巡检轮次 · 2026-09-11

按以下任务卡顺序实施，每卡完成后记录结果。恢复点：`output/checkpoints/frontend-before-delivery-20260911.zip`。

| ID | 范围 | 验收 | 状态 |
| --- | --- | --- | --- |
| R01 | 软件更新与认证响应复核 | 实际 API 契约；未知/失败/警告不得报最新；更新操作确认；版本夹具 | 已完成：类型/构建及13项浏览器断言；真实后端检查未执行 |
| R02 | Settings 配置与 N04 收口 | 显式字段映射、模块保存、读取失败禁写 | 已实施：5项字段行为测试、5项浏览器场景、类型/构建通过；账户与备份真实读写未验收 |
| R03 | 全部应用、桌面、共享 UI | 23 应用浅深色/390px巡检；状态、操作及弹层可达 | 已完成本轮：23应用、69组主截图、0破图/异常/根容器溢出；15项弹窗/支付夹具通过 |
| R04 | 交付工程 | 白名单源码包/静态包、安装说明、许可证来源、校验清单、干净目录复建 | 已完成：干净源码包安装/16测试/类型/构建通过；7项生产包浏览器夹具通过；Docker/真实后端未验收 |


## 当前轮次：全界面打磨（已收尾，2026-09-09）

用户已要求将桌面、图标和所有应用统一打磨至 macOS 正式版的视觉品质。此轮允许独立文件组并行；每组仍逐页理解和修改，不改变业务 API。

| ID | 范围 | 验收 |
| --- | --- | --- |
| P01 | 全局材质、字体、控件、窗口、菜单、控制中心 | 浅深色可读；只保留真实作用的设置；焦点与减少动效 |
| P02 | 图标注册表、统一图标组件、桌面/Dock/启动台/访达 | 光学尺寸、清晰度、标签、键盘入口一致 |
| P03 | 用户侧10个应用 | 逐页层级、密度、加载/错误/空态与窄屏 |
| P04 | 管理侧11个应用 | 保留Accounts标杆；统一列表与表单，保留业务路径 |
| P05 | 设置与访达 | 真正生效的外观偏好、响应式导航、真实身份/连接状态 |
| P06 | 全应用浏览器走查、生产包与交接 | 23个应用可打开，截图检查、既有交互回归、构建 |

**P01–P06 已完成。** 23 个应用已打开走查；真实后端联调、生产部署和支付仍不在本轮。

本轮恢复基线：`output/checkpoints/frontend-before-polish-20260908-2210.zip`。

更新时间：2026-09-10。当前轮次继续按任务卡逐页收尾，真实后端联调仍单独隔离。

**交付状态：F01–F06 已实施，F07 已完成类型/构建、11项窗口专项检查及15项浏览器断言；N02 已实施并完成接口夹具/浏览器验收，N03 已实施并完成使用记录夹具/浏览器验收。N04 已有读取前禁写与网关模块结果聚合实现，但完整浏览器保存流程仍待复跑；N05 尚未开始；真实后端读写仍未验收。** 详细结果见 [2026-09-09 交付记录](DELIVERY_2026-09-09.md)、[2026-09-10 交付记录](DELIVERY_2026-09-10.md) 和 [2026-09-11 收尾记录](DELIVERY_2026-09-11.md)。N06 的最小开发消费者已经实现，不要重复搭建。

## 最新目标

**前端质感效果优先，其次保证桌面框架可行、能对接原版 Sub2API。** 网关、支付实现和生产部署不是本轮必要项。此文件替代旧 `docs/REFACTOR_PLAN.md` 中的执行优先级。

技术路线固定：保留 Vue 3 + TypeScript + Pinia；保留现有 macOS 风格；先改善共享交互，再以账号页做标杆；业务 App 按需加载；提供独立组件预览验证可复用性。

## 当前任务卡

| ID | 任务 | 文件边界 | 状态 |
| --- | --- | --- | --- |
| F01 | 窗口几何、小屏、Pointer 拖拽/缩放、聚焦质感 | core 的 useWindowManager、MacWindow 及专用几何工具 | 已实施 |
| F02 | 通用 Sheet/Alert/Button，焦点与防重入 | core 的 MacSheet、MacAlertSheet、MacButton、modal composable | 已实施 |
| F03 | 账号管理标杆页：工具栏、表格、状态、分页和确认 | AccountsApp 及其局部组件 | 已实施 |
| F04 | Dock、菜单栏、桌面数据与外观偏好 | MacDock、MacMenubar、system、桌面组件、style | 已实施 |
| F05 | 按需加载、统一应用入口和会话清窗 | manifest、App、auth、加载/失败组件 | 已实施 |
| F06 | 原版后端配置入口，独立开发组件预览 | vite.config、环境样例、src/dev、main | 已实施 |
| F07 | 类型、构建、交互回归、截图和交付记录 | scripts 检查、output、本文档 | 已验证 |

每项验收：

- **F01**：390px 窗口不溢出；拖拽无过渡追赶；最大化→还原稳定；关闭窗口释放事件。
- **F02**：同屏两个确认框 Enter 只作用最上层；loading 不再触发；Tab 可循环；Escape 与关闭按钮一致；小屏可滚动。
- **F03**：加载/错误/空/有数据四态；搜索筛选和分页一致；删除有确认；默认账号类型符合 `apikey` 契约；保留既有 API。
- **F04**：桌面/窄屏 Dock 不越界；入口可键盘启动；状态未知显示占位；主题刷新后保留；浅深色可读。
- **F05**：首屏不静态加载 23 个应用；打开失败可恢复；退出/换号清窗；所有打开路径使用同一角色策略。
- **F06**：后端目标可配置且默认 `/api/v1` 契约不变；组件预览不访问业务 API；生产包不包含预览夹具。
- **F07**：记录实际运行的命令与结果；不把夹具测试写成原版后端验收；产物可打开。

## 后续模型执行规则

1. 先读本文件状态和 `DESIGN.md`，检查实际文件是否已经实现，不能根据旧审阅报告重复修改。
2. 选择一个未完成任务，先读目标文件、API 类型、同类已完成页面。写出“当前行为→目标行为→最多改哪些文件”。
3. 不扩大边界。发现网关、付款、数据库问题只登记，不切换主线；不要新增后端服务或新 UI 框架。
4. 使用现有组件与变量。每次只迁移一个页面或一组相关交互，不能批量正则替换所有页面。
5. 保持加载、错误、空、真实数据状态；演示数据只放 `src/dev` 或测试脚本，不得加进 API fallback。
6. 先类型检查，再构建，再浏览器检查。失败先修本任务引入的问题，不删除错误组件或降低 TypeScript 严格度。
7. 记录改动文件、检查结果、截图位置和剩余缺口，更新状态后才执行下一卡。

## 后续页面迁移任务

按 F03 标杆逐页推进，一次一页：

| ID | 页面与范围 | 必须检查的业务边界 | 完成标准 |
| --- | --- | --- | --- |
| N01 | Dashboard：图表、指标、错误态和窄屏布局 | 用户/管理员不同数据源；金额单位与时间范围 | 进行中：首屏、窗口复用回顶、空态密度和窄屏已复检；错误态与真实业务数据仍需专项验收 |
| N02 | API 密钥：主从详情、分页、编辑反馈 | token 展示/复制、到期、分组和额度字段 | 已实施并完成夹具验收；真实后端读写仍未验收：列表不静默截断；未改字段保留；长密钥不撑布局 |
| N03 | 使用记录：筛选条、表格密度、详情 | 原有 CSV 全量分页导出不能退化 | 已实施并完成夹具验收；真实后端读写仍未验收：筛选/分页可恢复，详情支持键盘入口，CSV 全量分页导出保留 |
| N04 | Settings：先外观与账户，再分模块 | 读取成功前禁写；表单映射严格使用API类型 | 每模块独立加载/保存，部分失败不报全量成功 |
| N05 | Users/Groups/Channels：复制标杆交互 | 保留各自payload与权限，不照抄账号字段 | 四态齐全、危险动作确认、窄屏操作可达 |
| N06（已完成） | 最小第二消费者示例 | core 不依赖业务 API/auth/store | 只提供AppDefinition和主题即可运行两个应用 |

### N01 最近修复记录（2026-09-09）

- 根因：Dashboard 内容滚动容器在窗口已存在时保留了旧的 `scrollTop`；再次点击 Dock 只保持 `isFocused=true`，原有监听无法感知这次激活，导致首组指标被裁掉。
- 变更：`mac-ui-core` 的窗口实例增加激活序号；Dashboard 在激活、视图切换和异步更新后统一回顶，并关闭滚动锚定。开发者中心空态图表收紧至 `210px`，底部日志/快捷操作使用同一标题栏高度规则。
- 浏览器复检：当前 `5173` 真实标签完成“打开 → 切换开发者中心 → 滚动至 `220px` → 再点同一 Dock 图标”流程；窄视口 `1163×717` 与正常视口均确认 `scrollTop=0`、首组指标可见、横向溢出为 `false`。管理员总览与开发者中心来回切换通过。
- 检查：`node --test packages/mac-ui-core/tests/window-manager.test.cjs`（11/11）；`pnpm --filter @sub2-mac/console typecheck`；`pnpm --filter @sub2-mac/console build`。
- 边界：上述是前端交互和布局证据，不等于原版后端业务数据、断网错误态或生产部署验收。

### N02 完成记录（2026-09-09）

- 变更文件：packages/sub2-console/src/apps/user/KeyChainApp.vue；N02 夹具脚本：scripts/keychain-browser-check.js。
- 结果：类型检查、生产构建和 22 项浏览器断言通过；第 1、2 页均被请求，夹具中的创建/更新/删除写请求为 0。
- 截图：output/playwright/n02/keychain-light.png、keychain-dark.png、keychain-mobile.png。
- 边界：业务请求全部由浏览器夹具拦截；这证明前端契约、状态和交互，不等于真实原版后端读写兼容性或生产验收。

### N03 完成记录（2026-09-10）

- 变更文件：packages/sub2-console/src/apps/user/ActivityApp.vue、packages/sub2-console/src/api/usage.ts、packages/sub2-console/src/api/groups.ts；N03 夹具脚本：scripts/activity-browser-check.js。
- 结果：`pnpm --filter @sub2-mac/console typecheck`、`pnpm --filter @sub2-mac/console build` 通过；浏览器夹具 21 项断言通过。覆盖 123 条筛选选项分页、205 条使用记录服务端分页、筛选参数、详情键盘入口、CSV 第 1/2/3 页完整导出、不完整导出错误与重试、用量/错误列表 503 保留旧行、辅助分组失败提示、错误详情重试和 390px 窄屏。
- 安全边界：夹具会拒绝所有非 GET 请求，本次 `mutationRequests=[]`；浏览器页面异常为 0。浏览器错误日志中的 503 和不完整导出日志均为主动注入的失败态验证，不代表生产故障。
- 截图：[浅色](../../output/playwright/n03/activity-light.png)、[深色](../../output/playwright/n03/activity-dark.png)、[窄屏](../../output/playwright/n03/activity-mobile.png)。
- 边界：业务请求全部由浏览器夹具拦截；这证明前端契约、状态和交互，不等于真实原版后端读写兼容性或生产验收。

N04–N05 仍是后续工作；N02、N03 已有独立交付记录，N06 已有两个应用的独立开发消费者和浏览器证据。2026-09-11 收尾审阅删除了无引用的一次性安装/登录/真实页面审计脚本，并修正了 API 解包后的管理员会话恢复。真实原版后端版本与集成环境仍需用户提供或从明确的项目来源核验；没有环境时只能标记“接口契约/夹具验证”。

## 恢复与边界

本轮改动前快照：`output/checkpoints/frontend-before-20260908-2054.zip`。仅包含前端源码及必要说明/构建文件。需要恢复时先对比单文件，不整体覆盖其他人的后续工作。

根目录旧静态原型和 `server/` 仍属于历史交付范围；本轮完成前端成果不等于这些设施通过生产准入。
