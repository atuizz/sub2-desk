# 正式发布准入核查 · 2026-09-13

后续执行已完成主要实现、真实核心闭环、nginx字节/TLS/WS、官方包升级及数据库恢复，当前执行结果以RELEASE_CLOSEOUT_20260913.md和release-verification.json为准。下文保留最初阻断清单作追踪，不能继续按旧“未做”描述本轮已完成项。Docker镜像/实际生产拓扑、供应商沙箱及GitHub限流的在线更新边界仍明确保留。

结论：目前可作为本地测试/候选版本，不满足“完整替代官方且可直接生产使用”的稳定版声明。此前“复核完成”指当轮指定范围的源码和夹具，不是全部官方功能或真实业务验收。

## 当前已核实

- E02三屏账号助理全测678项：677通过、1可选public浏览器跳过，类型/默认构建通过。对应日志output/account-editor-final/wizard-tests.txt、wizard-build.txt。大量测试是内存API/浏览器拦截夹具。
- 当前5173公共API真实返回后端0.2.1。最近一次官方稳定版核对为v0.2.4，来源和commit见CURRENT_UPSTREAM_20260912.md，不把9月12日的发行查询冒充本次实时查询。
- latest.json指向output/releases/1.0.0-20260912-180938，realBackendAccepted=false；E02三屏改动晚于该归档，现源码/默认dist不等于已发布候选包。

## 正式稳定版阻断项

1. **功能完整性未收口**：代码核查仍缺Gemini AI Studio新建（仅重授权识别已有类型）、混合渠道风险确认（有API而Accounts未消费）、部分请求头覆写/指纹/缓存TTL/Session ID masking/平台专有控制入口。原PARITY表中的MiniMax/国产协议/Grok媒体已完成，不再重复列缺失；官方删除的Session Token不算缺口。监控滚轮缩放/查询参数恢复、部分用量分组/端点/余额历史是其他已记录差异。
2. **目标后端真实业务验收缺失**：尚无0.2.4隔离真实环境完整证据，不能由0.2.1公共200推断新增接口兼容。账号创建/导入/更新/重授权、API key真实调用和扣费、兑换、订阅、配置生效应闭环验收；第三方OAuth/Passkey/验证码/支付SDK/Webhook/退款需相应测试配置。不要对当前实例随意执行不可逆操作。
3. **生产请求体限制冲突**：deploy/nginx.conf.template的client_max_body_size=20m，前端JSON单文件20MiB、合计50MiB（desktop-file-drop.ts），批量图片校验可达128MiB解码参考数据（useBatchImages.ts；HTTP JSON含base64体积更大）。即使单文件20MiB也可能因封装超过20m。正式入口会413；必须明确统一前端/代理/后端/支付和上传的限制并做边界请求验收。官方后端默认server请求体256MiB并不自动修改本地模板。
4. **生产部署及升级恢复未验收**：Docker/nginx模板未实跑，TLS/深链接/代理/上传/WebSocket/重启后恢复及日志需要验证。官方更新查询已观察过，但升级→健康→版本→业务回归→回滚/备份恢复尚无实证。
5. **冻结候选包及文档一致性**：待上述修复后，基于同一代码快照构建默认/release、重打包、校验hash、干净目录复建和生产包浏览器检查。发布说明统一支持的后端版本、平台/支付能力及明确限制；旧历史文档不能替代最终功能清单。Apple素材的本地版与可分发资源版继续分开。

## 推荐准入顺序

先完成源码级功能对照及部署限制修复；再用隔离的官方目标版本做真实核心业务闭环；随后验证可选供应商能力并明确未配置项；最后冻结/打包/复建/发布说明。所有真实业务与副作用验证需明确测试实例、测试账号和可恢复范围，不能以本地夹具数量代替。

本次为只读评估及记录，未修改生产配置、升级后端、执行支付或重打发布包。没有合理依据给出“100%完成”或剩余百分比。
