# R14 · 发行版与本地体验统一（素材与部署核验通过）

用户明确要求使用本地体验的图标与壁纸，覆盖此前独立原创发行素材方案。将本地使用的素材逐文件同步到public-release，保留Sub2 Desk网站品牌；启用一致的壁纸选项；发布1.1.1并重新截图。真实数据、配置、未使用的user-ref参考图不进入发行。来源说明改为事实记录，不将第三方素材宣称为原创或LGPL重新授权。验收以素材SHA256一致、发行包实际截图、Linux构建/部署检查为准。

130项素材与本地逐字节一致，Git提交内文件也逐个核对无差异；默认/发行同样开放4K壁纸。旧生成器改为仅输出实验目录，避免再次覆盖用户确认的外观。生产静态包重新截图4张，逐张视觉检查，页面异常0、破图0；GitHub README图片均加载成功。同期视频任务的执行计划改动保留未提交。

Linux CI 35041599058通过：类型、回归、release构建、真实前后端部署、登录和重建测试。部署检查额外通过HTTP读取130个素材并与本地清单逐个比对，防止只有仓库文件正确而容器仍使用旧图标。公开安装命令固定1.1.1；Published installer另测1.1.0升级、配置哈希和管理员身份保留。
