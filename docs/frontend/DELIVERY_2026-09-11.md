# 前端收尾记录 · 2026-09-11

> 后续巡检纠正：本文关于 auth.ts 重复解包的判断不成立，相关补丁已撤回。最新版本、UI、设置和发行验证见 DELIVERY_REVIEW_2026-09-11.md。不要沿用本文的阶段性结论作为最终验收。

## 任务汇总

当前 Codex 任务列表中只有本目录的“汇总进度并清理低质量代码”任务与 Sub2-Mac 相关；归档列表未发现另一个 Sub2-Mac 任务。项目任务状态以本执行计划和各日交付记录为准：F01–F07 已完成，N02/N03 已完成夹具验收，N04 有实现但完整浏览器收尾未完成，N05 未开始，N06 已完成开发消费者验证。

## 本次清理

- 删除 2026-09-09 05:21–05:31 生成且无引用的 `scripts/setup-admin.js`、`setup-finish.js`、`setup-next.js`、`setup-redis.js`、`setup-step.js`。
- 删除无引用的一次性 `scripts/frontend-login-check.js` 和 `scripts/real-page-audit.js`。前者包含固定登录试填，后者直接面向真实页面并包含固定测试账号，不适合作为长期项目脚本。
- 保留 `activity-browser-check.js`、`keychain-browser-check.js`、`settings-closeout-check.js` 及既有前端回归脚本，因为它们有任务卡记录或仍用于本地夹具验收。
- 修正 `packages/sub2-console/src/stores/auth.ts`：客户端已解包标准 `{ code, message, data }` 响应时，管理员会话恢复不再错误读取二级 `.data`；同时兼容旧 Axios 响应形态。

## 验证

- 保留脚本 `node --check`：通过。
- `pnpm --filter @sub2-mac/console typecheck`：通过。
- `pnpm --filter @sub2-mac/console build`：通过。
- 专用 Vite 实例和 Playwright 浏览器尝试：修复后确认管理员导航可出现；完整 Settings 四模块流程因专用开发服务器后续退出未稳定完成，不能记为通过。

## 未完成项与边界

- N04 仍需在稳定的浏览器夹具会话中验证读取失败不写入、单模块失败汇总和全模块成功提示。
- N04 的原版后端字段映射、真实读写、权限和生产部署仍未验收；当前 Settings 仍有历史字段映射与整对象表单状态混用问题，不能把构建通过写成业务保存正确。
- N05 Users/Groups/Channels 尚未开始。本次没有改 `server/`、根目录二进制、真实配置或旧原型。
