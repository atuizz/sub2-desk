# 前端交付记录 · 2026-09-10

## N03 · 使用记录

判断：N03 已实施并完成前端结构、交互和本地接口夹具验收；真实原版 Sub2API 后端的读写兼容性仍未验收。

### 实际变更

- packages/sub2-console/src/apps/user/ActivityApp.vue
  - 使用记录、统计、模型分布、趋势图和错误列表请求增加 AbortController、版本隔离与卸载保护；筛选条件加载失败时保留可用内容并显示辅助提示。
  - 保留既有筛选、日期范围、排序、分页和详情路径；服务端列表失败时保留上一份有效行，不再把短暂失败渲染成空数据。
  - CSV 导出按服务端总量分页读取全部页面，显示进度；发现返回不完整时阻止生成不完整文件并提供重试入口。
  - 使用记录和错误请求表格行支持 Tab、Enter、Space 打开详情；错误详情加载失败时提供可重试状态；补充筛选控件、刷新、导出和详情的可访问性标签。
  - 增加 760px/520px 容器断点，整理窄屏工具栏、筛选面板和表格焦点样式，保持浅色/深色 macOS 风格。
- packages/sub2-console/src/api/usage.ts、packages/sub2-console/src/api/groups.ts
  - 为统计、模型、快照、错误列表、错误详情和可用分组 API 保留原契约并增加可选 AbortSignal 传递。
- scripts/activity-browser-check.js
  - 新增 N03 专项浏览器夹具；业务 API 全部由夹具响应，并拒绝所有非 GET 请求。

### 验证证据

- `pnpm --filter @sub2-mac/console typecheck`：通过。
- `pnpm --filter @sub2-mac/console build`：通过，生成最新 `packages/sub2-console/dist/`。
- `node --check scripts/activity-browser-check.js`：通过。
- Playwright CLI 夹具验收：21 项断言通过，包含：
  - 123 条 API 密钥筛选选项跨第 1、2 页加载；205 条使用记录按服务端分页读取。
  - API 密钥、日期范围和错误分类筛选发出参数；列表分页和表格行键盘入口可用。
  - CSV 正常导出请求第 1/2/3 页；不完整返回显示错误且可重试。
  - 用量列表、错误列表 503 保留已有行；分组辅助接口失败只显示警告；错误详情失败可重试。
  - 浅色、深色和 390px 窄屏截图；未产生写请求；页面异常为 0。
- 截图：
  - `output/playwright/n03/activity-light.png`
  - `output/playwright/n03/activity-dark.png`
  - `output/playwright/n03/activity-mobile.png`

### 边界与下一步

- 所有业务 API 在浏览器中由夹具响应；本次没有执行真实创建、更新或删除，也没有加入业务 fallback 假数据。
- 夹具主动注入的 503 和不完整导出错误会在浏览器控制台留下预期错误日志；这些日志用于验证失败态，不代表生产故障。
- 构建和夹具通过只证明前端源码、API 参数映射和交互链路通过，不证明真实后端版本、登录/TOTP、权限、数据一致性或生产部署兼容。
- 下一张任务卡为 N04「Settings」：按模块处理外观、账户和独立加载/保存失败态。
