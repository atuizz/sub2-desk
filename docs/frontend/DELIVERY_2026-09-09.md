# 前端交付记录 · 2026-09-09

## N02 · API 密钥

判断：N02 已实施并完成前端结构、交互和本地接口夹具验收；真实原版 Sub2API 后端的读写兼容性仍未验收。

### 实际变更

- packages/sub2-console/src/apps/user/KeyChainApp.vue
  - 列表刷新增加 AbortController、版本隔离和卸载保护；分页读取全部服务端页面，不再静默只展示前 100 条。
  - 将密钥列表作为主请求，分组、公共设置、用量作为可见的辅助失败状态；辅助接口失败时保留可用列表并给出重试提示。
  - 增加名称、自定义密钥、额度、限流、有效期校验；状态切换、快捷换组和删除增加统一防重入，分组更新失败恢复原选择。
  - 创建表单保留一组 IP/限流/有效期字段；编辑表单补齐 IP 白名单/黑名单、5 小时/1 天/7 天限流和到期时间。
  - 额度耗尽/已过期状态在编辑表单中只读；接入示例统一使用当前复制内容，cURL 复制结果保留真实换行。
  - 完善空态、辅助警告、Toast 可访问性、长值布局和窄屏操作标签。
- scripts/keychain-browser-check.js
  - 新增 N02 专项浏览器夹具，不写入真实后端；夹具只计数并拦截创建、更新、删除请求。

### 验证证据

- pnpm --filter @sub2-mac/console typecheck：通过。
- pnpm --filter @sub2-mac/console build：通过，生成最新 packages/sub2-console/dist/。
- node --check scripts/keychain-browser-check.js：通过。
- Playwright CLI 夹具验收：22 项断言通过，包含：
  - 123 条密钥跨第 1、2 页完整加载；名称、状态、分组筛选。
  - 完整密钥复制；长密钥不撑破外层应用布局。
  - cURL 复制内容为 7 行真实换行；创建表单字段无重复。
  - 编辑表单 IP/限流/到期字段；额度耗尽状态不可手动切换。
  - 删除先确认，取消不发送 DELETE；503 刷新保留已有行。
  - 分组、用量、公共设置辅助失败只显示警告；Sheet Tab/Escape；浅色、深色和 390px 窄屏。
  - 未预期页面异常：0；创建/更新/删除写请求：0。
- 截图：
  - output/playwright/n02/keychain-light.png
  - output/playwright/n02/keychain-dark.png
  - output/playwright/n02/keychain-mobile.png

### 边界与下一步

- 所有业务 API 在浏览器中由夹具响应；本次没有执行真实创建、更新或删除，也没有把夹具数据加入业务 fallback。
- 构建通过只证明前端源码和打包链路通过，不证明真实后端版本、登录/TOTP、权限和生产部署兼容。
- 下一张任务卡为 N03「使用记录」：保留原有全量分页 CSV 导出、搜索/筛选/分页恢复路径和详情面板。
